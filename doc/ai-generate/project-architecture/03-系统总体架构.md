## 3. 系统总体架构

### 3.1 总体架构图

```mermaid
flowchart TB
    subgraph Client[接入端]
        T[教师端 Web]
        S[学生端 H5 / WebView]
        P[泛雅 / 学习通平台]
    end

    T --> FE[Vue 前端应用]
    S --> FE
    P --> API
    FE --> N[Nginx / 静态资源与反向代理]
    N --> API[FastAPI 应用服务]

    API --> A1[平台接入与鉴权]
    API --> A2[课件 / 脚本 / 发布 API]
    API --> A3[问答 / 进度 API]
    API --> A4[评测与管理 API]

    API --> W[Dramatiq Worker]

    W --> P1[课件解析]
    W --> P2[CIR 构建]
    W --> P3[全文索引与检索辅助]
    W --> P4[TTS / 发布装配]

    API --> DB[(MySQL)]
    API --> R[(Redis)]
    API --> O[(MinIO / S3)]
    API --> LLM[LLM API]
    API --> TTS[TTS API]
    API --> ASR[ASR API]

    W --> DB
    W --> R
    W --> O
    W --> LLM
    W --> TTS
```
### 3.2 业务闭环图

```mermaid
flowchart LR
    U[教师上传 PPT/PDF] --> P1[异步解析]
    P1 --> P2[页级标准化]
    P2 --> P3[CIR / 节点树]
    P3 --> P4[LLM 生成讲稿]
    P4 --> P5[教师编辑]
    P5 --> P6[生成音频]
    P6 --> P7[发布 LessonPackage]
    P7 --> L1[学生开始学习]
    L1 --> Q1[文字 / 语音提问]
    Q1 --> Q2[证据约束问答]
    Q2 --> Q3[理解度判断]
    Q3 --> R1[续讲 / 补讲 / 回到前置节点]
    R1 --> L1
```

### 3.3 运行形态定稿

该运行形态直接服务于 `requirements-analysis/` 中已经确认的主链路需求：先跑通“解析 -> 生成 -> 问答 -> 续接”的教学闭环，再保证平台接入、安全和指标证据。

正式运行形态固定为：

一个前端应用：Vue 3 + Vite，内部按教师端 / 学生端分路由与布局；

一个后端主服务：FastAPI，对外提供 /api/v1 统一接口；

一个异步 Worker：处理耗时任务；

三类基础存储：MySQL、Redis、MinIO / S3；

三类外部 AI 能力：LLM、TTS、ASR；

一套评测与监控能力：指标、日志、黄金样本、性能统计。
