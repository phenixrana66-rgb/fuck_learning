@baseUrl = http://127.0.0.1:8000
@parseId = parse20260412131029767c1e

### 结构化讲稿生成（先完成 lesson-parse，再把 parseId 填到这里）
POST {{baseUrl}}/api/v1/lesson/generateScript
Content-Type: application/json

{
  "parseId": "{{parseId}}",
  "teachingStyle": "standard",
  "speechSpeed": "normal",
  "customOpening": "null",
  "enc": "demo-signature"
}
