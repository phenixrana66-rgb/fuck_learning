# fuck_learning

前端入口已统一到仓库根目录。

开发：

```bash
npm install
npm run dev
```

构建：

```bash
npm run build
```

学生端后端仍位于 `student-ai-course/backend/student_plugin`，教师端 mock 仍位于 `teacher-ai-course/mock/server.js`。
